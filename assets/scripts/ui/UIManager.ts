// UIManager placeholder (TypeScript)
// Path: assets/scripts/ui/UIManager.ts

export default class UIManager {
  showMainMenu() {
    // show start button
  }

  showHUD(score: number) {
    // update score display
  }

  showGameOver(score: number, onRevive: () => void) {
    // show gameover UI with revive button
  }
}
